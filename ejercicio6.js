/***
 * Recorre la listra de frutas
 * y verduras utilizando un bucle for
 * y muestra cada elemento en la consola.
 */

for(var i=0; i< nombreFRutas.length; i++ ){
    alert( nombreFrutas[i]);
}


/***
 * Recorre la listra de frutas
 * y verduras utilizando un bucle for
 * y muestra cada elemento en la consola.
 */
var i = 0;
while( i < nombreFrutas.length ){

    alert(nombreFrutas[i]);
}
